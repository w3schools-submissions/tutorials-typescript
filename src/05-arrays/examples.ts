const arr: string[] = [];
arr.push("item");

// Readonly
const names: readonly string[] = ["Dylan", "Alice", "Bob"];
names.push("Jack");

// Type Interface
const numbers: = [1, 2, 3];
numbers.push(4);
numbers.push("5");
let head: number = numbers[0];
